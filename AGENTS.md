# 神里绫华 Agent — 项目总纲

## 项目定位（最终目标）

构建一个打通 AI 领域上下游的**通用平台**，覆盖「模型训练 → 模型管理/编排 → 实际应用」的全流程。

### 上游 — 模型训练（源头）
- 由用户提供资源，训练**学习性质**的简单模型（重在理解原理，不追求生产实用性）：
  - 大语言模型（LLM）—— 小规模复现
  - 文生图模型
  - 文生视频模型
- 数据训练 AI 语音（参考目录内 `GPT-SoVITS-v2pro-*/`）

### 中游 — 管理与编排（平台核心）
- 自主实现的 **harness**（模型管理、生命周期、评估）
- **工作流设计平台**（可视化编排多模型 / 多工具）

### 下游 — 应用交付（落地）
- 通过调用 API 实现最终功能，**以调用成熟模型 API 为主**：
  - 对话（chat）
  - Vibe Coding（AI 编程）
  - 文本转语音（TTS）
  - 文生图
  - 文生视频

## 架构与部署决策

- **部署形态**：先在云服务器部署（Linux Ubuntu 24.04），采用**静态前端 + 后台 API** 框架
- **用户体系**：短期仅自己使用，只做最简注册 / 登录并预留扩展；完整多用户与计费后置
- **形态演进路线**：Web 网站（先，待下游成熟后购买域名）→ 桌面软件 → 移动端 App（会阉割部分功能）
- **建设顺序**：下游 → 中游 → 上游（上游训练作为「实验室模块」通过中游 harness 接入）
- **美术主题**：全程保持「神里绫华（Kamisato Ayaka）」作为视觉 / 角色主题，不可丢弃

## 技术栈（详见 `WEB_PLAN.md`）

- 前端：Vite + React 19 + TypeScript + Tailwind + shadcn/ui（纯 SPA，不用 Next.js）
- 后端：Python FastAPI
- 数据：PostgreSQL + Redis；部署：Docker Compose + Nginx（Ubuntu 24.04）

## 当前状态

- 已有 `WEB_PLAN.md`：Web 方案（当前主方向，下游优先 + 模型网关）
- P0 脚手架已搭好：`frontend/`（Vite+React+Tailwind）、`backend/`（FastAPI）、`deploy/`（docker-compose + nginx）
- 已有 `PLAN.md`：桌面桌宠 MVP 规划（Electron + Live2D + LLM，后期软件形态参考）
- `参考项目/`：集中存放外部参考项目（后续可继续加入）
  - `Cyrene-Agent/`（开源桌宠架构，下游参考）
  - `GPT-SoVITS-v2pro-*/`（语音训练，上游参考）
- 桌宠（Electron 桌面形态）是下游「对话」的一种落地形态，属后期扩展

## 重要约定

- **长期项目**：上述为初定最终目标，分阶段推进，后续用户会逐步描述当前具体工作
- 训练部分定位为「学习 / 复现」，生产推理以成熟 API 为主
- 以用户最新指示为准，避免一次性铺开全范围

## 版本管理与发布流程

- 版本管理用 git，远程仓库：https://github.com/xianqianqianzz/Ayaka-agent
- 同步流程：本地 git → push GitHub → 云服务器从 GitHub 拉取部署（Ubuntu 24.04）
- 阶段节奏：每个阶段完成后，先由用户完成云服务器同步与测试，再进入下一阶段

## 待办 / 待学习

- 进入中游前，补齐中游领域知识（模型资产管理 vs 工作流编排等概念）
